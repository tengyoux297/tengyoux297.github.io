# CV source

This directory contains the LaTeX source for the website CV PDF.

Build locally from this directory:

```powershell
.\build.ps1
```

The generated PDF lives at `out/cv.pdf`, and the website links to `/cv/out/cv.pdf`.
